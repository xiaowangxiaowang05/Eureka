Here is the environment's observation code:
{task_obs_code_string}


Here is the task description: {task_description}


YOUR TASK: Write a Reward Function

Based on the code and description, write a reward function.

1. Identify Available Variables:
   - Look at the `_obs.py` code provided above. You can use any variable defined there (e.g., `self.object_rot`, `self.dof_pos`, etc.).
   - You can also use common variables inherited from the base environment, such as:
     - `self.progress_buf`: The current step count.
     - `self.object_angvel`: Object angular velocity.
     - `self.object_linvel`: Object linear velocity.
   (Note: Only use variables relevant to the task).

2. Define Function Arguments (CRITICAL):
   - You MUST define these variables as inputs in the `compute_reward` function signature.
   - DO NOT access `self.variable_name` inside the function body. Use the argument name directly.

   **WRONG (Will fail):**
   ```python
   def compute_reward(self):
       return torch.norm(self.object_pos - self.goal_pos)